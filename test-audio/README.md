Place local WAV files here while testing the app.

The repository does not include generated audio fixtures by default. Install the Python requirements before using a WAV file to exercise the Electron-to-Python-to-MIDI flow.
