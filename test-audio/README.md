Place local WAV files here while testing the app.

The repository does not include generated audio fixtures by default. The Python backend has a fallback transcription, so any existing WAV file can exercise the Electron-to-Python-to-MIDI flow even before Basic Pitch is installed.
